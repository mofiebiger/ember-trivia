<div local-class="ui-header-container {{if (eq @style "ui-secondary-header") "ui-secondary-header"}}">
  {{#if @text}}
    <p data-test-header-text>{{@text}}</p>
  {{else}}
    <img
      data-test-header-img
      local-class="ui-header-img"
      src={{@src}}
      alt={{@alt}}
    />
  {{/if}}
</div>
{{yield}}
