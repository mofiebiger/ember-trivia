<QuestionCard
    @id={{this.model.id}}
    @question={{this.model.question}}
    @correctAnswer={{this.model.correct_answer}}
    @options={{this.model.options}}
    @score={{this.model.score}}
    @buttonText="Next Question"
    @route="questions.question-two"
 />
